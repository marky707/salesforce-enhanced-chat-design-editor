# 05_formal-review/input/

Ready packages awaiting the accountable human decision, per review ID subfolder.

Start with `<ID>-formal-review-packet-round-<NN>.md`. The top level contains current/actionable material only: current design and supporting artifacts, ready review, authoritative `<ID>-open-decision-index-round-<NN>.md`, structured residual risks on the cover, the run-specific `<ID>-formal-decision-round-<NN>-FORM.md`, and `<ID>-current-artifacts-round-<NN>.md`. The ledger's current round supplies `<NN>`. The stable stage template is not copied into the packet.

Earlier reviews, responses, drafts, and superseded artifact versions stay in `history/`. They remain available for audit but are not competing action surfaces.
